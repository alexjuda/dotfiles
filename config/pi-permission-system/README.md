[Plugin website](https://pi.dev/packages/@gotgenes/pi-permission-system).

```bash
pi install npm:@gotgenes/pi-permission-system
```

The config file should be symlinked to `~/.pi/agent/extensions/pi-permission-system/config.json`. Annoyingly Pi doesn't follow XDG so configs are interleaved with generated files.
