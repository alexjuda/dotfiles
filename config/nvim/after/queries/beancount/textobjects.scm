;; Allows navigating between sections using Treesitter Text Objects.
;;
;; Unfortunately, `@class.inner` would only capture individual section children instead of the section body as a whole.
(section) @class.outer
