# Ansible configuration for localhost management
# Created: 2025-05-01
# Purpose: Standard ansible config for Fedora workstations
#
# Usage: Run `make localhost` (which uses sudo ansible-playbook)
# For fresh machines: No special setup needed

[collections]
collections_paths = ./.collections
