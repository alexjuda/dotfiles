# based (in general) on https://gitlab.com/dwt1/dotfiles/-/blob/master/.config/alacritty/alacritty.yml

env:
    # Declare alacritty as a regular `xterm-256color` for better compatibility with 
    # tools that need to run with other terminals with the same config, like `tmux`.
    # Defaults to `alacritty`.
    # from https://github.com/alacritty/alacritty/issues/109#issuecomment-507026155
    TERM: xterm-256color

font:
    normal:
        family: JetBrains Mono
        style: Regular
    size: 18

    # Character padding / line height
    offset:
        y: 10

    # Character baseline
    glyph_offset:
        y: 3 

window:
    # hide window title bar
    # based on https://github.com/alacritty/alacritty/issues/168
    # decorations: "full"
    # decorations: "transparent"
    # decorations: "buttonless"
    decorations: "none"

    padding:
        # window margin
        x: 10
        y: 6
    
key_bindings:
    # Make Alt-Left, Alt-Right move between words
    - { key: Right, mods: Alt, chars: "\x1BF" }
    - { key: Left,  mods: Alt, chars: "\x1BB" }

import:
    - ~/.config/alacritty/themes/dracula.yml

#colors:
#    # Doom One color scheme (https://github.com/eendroroy/alacritty-theme/blob/master/themes/doom_one.yml)
#    #
#    # Default colors
#    primary:
#        background: '0x282c34'
#        foreground: '0xbbc2cf'

#    # Normal colors
#    normal:
#        black:   '0x282c34'
#        red:     '0xff6c6b'
#        green:   '0x98be65'
#        yellow:  '0xecbe7b'
#        blue:    '0x51afef'
#        magenta: '0xc678dd'
#        cyan:    '0x46d9ff'
#        white:   '0xbbc2cf'

